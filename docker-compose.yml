services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  worker:
    build: ./apps/worker
    env_file:
      - .env
    depends_on:
      - redis
    ports:
      - "8000:8000"
